# Preprocessing

**STATUS:** BLOCKED

All preprocessing operations are halted because the `IO-VNBD` dataset is missing. We cannot document inputs, outputs, or transformations until real data structures are discovered.
